# pnpm security settings
# Solo "canvas" tiene permiso de ejecutar scripts postinstall/build.
# Cualquier otro paquete que intente correr lifecycle scripts será bloqueado.
only-built-dependencies[]=canvas
